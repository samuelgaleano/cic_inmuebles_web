allowBuilds:
  # Ambos traen binarios precompilados por plataforma; no necesitan su script
  # de instalacion. Se mantienen desactivados (igual que en produccion).
  sharp: false
  unrs-resolver: false
